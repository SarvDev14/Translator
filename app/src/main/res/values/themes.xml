<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Translator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Translator" parent="Base.Theme.Translator" />

    <style name = "Theme.App.Splashscreen" parent = "Theme.SplashScreen">

        <item name="windowSplashScreenBackground">@color/platunum</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_noback</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.Translator</item>
    </style>
</resources>